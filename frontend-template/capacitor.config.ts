import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.frontendtemplate.app',
  appName: 'frontend-template',
  bundledWebRuntime: false,
  webDir: "out"
};

export default config;
